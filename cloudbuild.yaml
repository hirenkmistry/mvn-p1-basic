steps:
- name: 'maven:3.8.1-jdk-11'
  entrypoint: 'mvn'
  args: ['clean', 'package']

artifacts:
  objects:
    location: 'gs://gcp-vagile-1-bucket-1'
    paths: ['target/*.war']

options:
  logging: CLOUD_LOGGING_ONLY
